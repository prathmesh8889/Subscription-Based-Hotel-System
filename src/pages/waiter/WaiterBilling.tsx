import React,{useEffect,useState}from'react';import{useAuth}from'../../context/AuthContext';import{api}from'../../services/api';export function WaiterBilling(){const{user}=useAuth();const[orders,setOrders]=useState<any[]>([]),[err,setErr]=useState('');const load=()=>user?.hotelId&&api<any>('/billing/unpaid?hotelId='+user.hotelId).then(r=>setOrders(r.data.orders)).catch(e=>setErr(e.message));useEffect(()=>{load()},[user?.hotelId]);const pay=async(id:string,m:string)=>{try{await api('/billing/pay/'+id,{method:'POST',body:JSON.stringify({paymentMethod:m})});load()}catch(e:any){setErr(e.message)}};return <div className="space-y-4"><h1 className="text-2xl font-bold">Billing & Payments</h1>{err&&<p className="text-red-600">{err}</p>}{orders.map(o=><div key={o.id} className="bg-white border rounded-xl p-4 flex justify-between"><div><p className="font-semibold">Table {o.table?.tableNumber} • #{o.id.slice(-6)}</p><b>₹{Number(o.totalAmount)}</b></div><div className="flex gap-2">{['CASH','UPI','CARD'].map(m=><button key={m} onClick={()=>pay(o.id,m)} className="bg-amber-500 text-white px-3 py-2 rounded">{m}</button>)}</div></div>)}</div>}