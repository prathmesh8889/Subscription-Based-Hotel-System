import React,{useState}from'react';import{useNavigate,useLocation}from'react-router-dom';import{useAuth}from'../context/AuthContext';import{UtensilsCrossed,Eye,EyeOff,AlertCircle,Loader}from'lucide-react';export function LoginPage(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false),{login}=useAuth(),nav=useNavigate(),loc=useLocation();const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');const r=await login(email,password);setLoading(false);if(!r.success){setError(r.error||'Login failed');return}const from=(loc.state as any)?.from?.pathname;if(from){nav(from,{replace:true});return}nav(r.role==='OWNER'?'/owner':r.role==='KITCHEN'?'/kitchen':r.role==='WAITER'?'/waiter':'/platform/dashboard',{replace:true})};return <div className="min-h-screen bg-slate-900 grid place-items-center p-4"><form onSubmit={submit} className="bg-white rounded-2xl p-5 sm:p-8 w-full max-w-md shadow-xl"><div className="text-center mb-6"><UtensilsCrossed className="mx-auto text-amber-500" size={44}/><h1 className="text-xl sm:text-2xl font-bold mt-2">RestroFlow</h1><p className="text-gray-500">Sign in to your restaurant account</p></div>{error&&<div className="mb-4 p-3 bg-red-50 text-red-700 rounded flex gap-2"><AlertCircle size={18}/>{error}</div>}<label className="text-sm">Email<input className="w-full border rounded-lg p-3 mt-1 mb-4" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="text-sm">Password<div className="relative"><input className="w-full border rounded-lg p-3 mt-1" type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-4">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><button disabled={loading} className="w-full mt-5 bg-amber-500 text-white rounded-lg p-3 font-semibold">{loading?<Loader className="animate-spin mx-auto"/>:'Sign in'}</button></form></div>}