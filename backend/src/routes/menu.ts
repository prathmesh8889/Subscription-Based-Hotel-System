import express from'express';import{getMenuItems,createMenuItem,updateMenuItem,deleteMenuItem}from'../controllers/menuController';import{authenticateToken,authorizeRole}from'../middleware/auth';const r=express.Router();r.use(authenticateToken);r.get('/',authorizeRole(['OWNER','KITCHEN','WAITER']),getMenuItems);r.post('/',authorizeRole(['OWNER']),createMenuItem);r.put('/:id',authorizeRole(['OWNER']),updateMenuItem);r.delete('/:id',authorizeRole(['OWNER']),deleteMenuItem);export default r;